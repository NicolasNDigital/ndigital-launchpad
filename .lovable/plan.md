

## Plan : Mise à jour du sitemap.xml (suppression du www)

Remplacer toutes les occurrences de `https://www.ndigital-pro.fr/` par `https://ndigital-pro.fr/` dans les 6 balises `<loc>` du fichier `public/sitemap.xml`.

Aucun autre changement (priorities, dates, changefreq restent identiques).

